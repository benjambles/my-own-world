// import { createMockContext } from '@shopify/jest-koa-mocks';
// import { isUser } from '../is-user.js';

// test('isUser', () => {
//     expect(isUser(createMockContext({ state: { user: { sub: 'some-key' } } }))).toEqual(
//         true,
//     );
//     expect(isUser(createMockContext({ state: {} }))).toEqual(false);
//     expect(isUser(createMockContext({ state: { user: {} } }))).toEqual(false);
//     expect(isUser(createMockContext({}))).toEqual(false);
// });
